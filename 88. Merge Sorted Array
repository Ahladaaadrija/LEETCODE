class Solution(object):
    def merge(self, nums1, m, nums2, n):
        t = m+n
        for i in range(n):
            for j in range(t):
                if nums1[j] == 0:
                    nums1[j],nums2[i] = nums2[i],nums1[j]
        nums1.sort()
        return nums1
        
