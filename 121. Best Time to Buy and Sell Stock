class Solution(object):
    def maxProfit(self, prices):
        min_price = float('inf')
        max_pro = 0
        for price in prices:
            if price < min_price:
                min_price = price
            else:
                profit = price - min_price
                if profit > max_pro:
                    max_pro = profit

        return max_pro
