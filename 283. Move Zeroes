class Solution(object):
    def moveZeroes(self, nums):
        n = len(nums)
        for  i in range(n):
            if 0 in nums:
                nums.append(nums.pop(nums.index(0)))
        return nums 
