class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        n = len(nums)
        if n == 0:
            return 0
        else:
            for i in nums:
                if nums.count(i) == 1:
                    return i
            return -1
        
